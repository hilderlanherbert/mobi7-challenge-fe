import { FormatPeriodPipe } from './format-period.pipe';

describe('FormatPeriodPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPeriodPipe();
    expect(pipe).toBeTruthy();
  });
});
